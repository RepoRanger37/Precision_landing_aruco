ekf_filter_node:
  ros__parameters:
    use_sim_time: false
    frequency: 30.0
    sensor_timeout: 0.5
    two_d_mode: false
    
    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: map

    publish_tf: true

    imu0: /drone_imu
    imu0_config: [false, false, false,
                  #false, false, false,
                  true, true, false,
                  false, false, false,
                  false, false, false,
                  #true, true, false,
                  false, false, false]
    imu0_differential: false

    odom1: /rf_distance_odom
    odom1_config: [false, false, true,   # only z
                   false, false, false,
                   false, false, false,
                   false, false, false,
                   false, false, false]
    odom1_differential: false
